[remote] kogen build -p kogen.cue
[remote] cmp stdout golden.yaml

-- kogen.cue --
package kube

kogen: nginx: {
    apiVersion: "kogen.internal/v1alpha1"
    kind: "Cog"
    spec: {
        resource: ["https://raw.githubusercontent.com/kubernetes/examples/master/guestbook/frontend-service.yaml"]
    }
}

-- golden.yaml --
